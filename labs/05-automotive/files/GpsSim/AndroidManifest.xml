<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aospi.gpsim">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- signature|privileged: only granted because this app is platform-signed -->
    <uses-permission android:name="android.permission.MOCK_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:label="GpsSim"
        android:theme="@style/Theme.AppCompat.Light">
        <activity
            android:name=".GpsSimActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
